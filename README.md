# 🍄 YOLO Fine-tuning - Детекція грибів

Проект для детекції грибів (BB та WB) за допомогою YOLO (You Only Look Once) моделі.

## 📋 Опис датасету

Датасет містить 2 класи грибів для детекції:
- **BB** - Коричневі гриби (Brown Button)
- **WB** - Білі гриби (White Button)

Датасет розділений на 3 частини:
- **train** - Тренувальний набір
- **valid** - Валідаційний набір
- **test** - Тестовий набір

## 🚀 Швидкий старт

### 1️⃣ Клонування репозиторію

```bash
git clone <your-repo-url>
cd yolo-fine-tuning
```

### 2️⃣ Скачування датасету

Датасет розташований за посиланням:
👉 [Скачати датасет](https://drive.google.com/file/d/1iPANwP1k6tbz1EZRdG2qbst1tqqYsZ_x/view)

**Інструкції:**
1. Перейдіть за посиланням
2. Натисніть кнопку "Download" (Скачати)
3. Розпакуйте архів у папку `data/`:

```bash
# Приклад: якщо ви скачали файл dataset.zip
unzip dataset.zip -d data/
```

Після розпакування структура повинна виглядати так:
```
data/
├── dataset.yaml
├── images/
├── train/
│   ├── images/
│   ├── labels/
│   └── depth/
├── valid/
│   ├── images/
│   ├── labels/
│   └── depth/
└── test/
    ├── images/
    ├── labels/
    └── depth/
```

### 3️⃣ Встановлення залежностей

```bash
# Створити віртуальне середовище (опціонально, але рекомендується)
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Встановити залежності
pip install -r requirements.txt
```

### 4️⃣ Тренування моделі

```bash
python train.py
```

**Що відбуватиметься:**
- Модель буде тренуватися на вашому датасеті
- Прогрес буде виводитися в консоль
- Результати зберігатимуться в папці `runs/detect/train/`
- Натренована модель буде збережена як `runs/detect/train/weights/best.pt`

### 5️⃣ Тестування моделі

```bash
# Повне тестування з метриками
python test.py

# Або швидке тестування
python quick_test.py
```

**Результати включають:**
- mAP50 - точність детекції при IoU=0.5
- mAP50-95 - точність детекції при IoU від 0.5 до 0.95
- Precision - частина правильних виявлень
- Recall - частина виявлених об'єктів

### 6️⃣ Прогнозування на новому зображенні

```bash
python predict.py
```

Або для конкретного зображення:

```bash
python -c "from ultralytics import YOLO; model = YOLO('runs/detect/train/weights/best.pt'); model.predict(source='image.jpg', conf=0.25, save=True)"
```

## 📁 Структура проекту

```
yolo-fine-tuning/
├── data/                      # Датасет
│   ├── dataset.yaml          # Конфігурація датасету
│   ├── train/                # Тренувальний набір
│   ├── valid/                # Валідаційний набір
│   └── test/                 # Тестовий набір
├── train.py                  # Скрипт для тренування
├── test.py                   # Скрипт для тестування з метриками
├── quick_test.py             # Скрипт для швидкого тестування
├── predict.py                # Скрипт для прогнозування
├── requirements.txt          # Залежності проекту
├── README.md                 # Цей файл
├── .gitignore                # Файли для ігнорування в git
├── yolov8n.pt               # Попередньо натренована модель YOLOv8
├── yolo11n.pt               # Попередньо натренована модель YOLO11
├── image.jpg                # Приклад зображення
├── image2.jpg               # Приклад зображення
└── runs/                     # Результати тренування та інференції
    └── detect/
        ├── train/           # Результати тренування
        ├── predict/         # Результати прогнозування
        └── test_predictions/# Результати тестування
```

## 🛠️ Розв'язання проблем

### Проблема: "ModuleNotFoundError: No module named 'ultralytics'"
**Рішення:**
```bash
pip install -r requirements.txt
```

### Проблема: "No such file or directory: 'data/dataset.yaml'"
**Рішення:** Переконайтеся, що датасет був правильно розпакований у папку `data/`

### Проблема: CUDA помилка або GPU не знайдена
**Рішення:** Модель автоматично використає CPU, якщо CUDA недоступна. Тренування буде повільніше.

### Проблема: Недостатньо пам'яті
**Рішення:** Зменшіть `batch` у файлі `train.py`:
```python
results = model.train(
    batch=8,  # Зменшіть з 16 на 8 або менше
    # ... інші параметри
)
```

## 📊 Метрики

Під час тестування ви побачите:

| Метрика | Опис |
|---------|------|
| **mAP50** | Середня точність при IoU=0.5 (більше = краще) |
| **mAP50-95** | Середня точність при IoU від 0.5 до 0.95 (більше = краще) |
| **Precision** | % правильних виявлень (більше = краще) |
| **Recall** | % виявлених об'єктів (більше = краще) |

## 🔄 Workflow

```
1. Скачати датасет
        ↓
2. Встановити залежності
        ↓
3. Запустити тренування (train.py)
        ↓
4. Оцінити результати (test.py)
        ↓
5. Використовувати на своїх зображеннях (predict.py)
```

## 💻 Системні вимоги

- Python 3.8+
- pip (менеджер пакетів Python)
- NVIDIA GPU (опціонально, для прискорення)
- 8+ GB ОЗУ
- 10+ GB вільного місця на диску

## 📦 Залежності

Всі залежності вказані в `requirements.txt`:
- ultralytics - бібліотека YOLO
- torch - PyTorch (глибоке навчання)
- torchvision - комп'ютерний зір
- opencv-python - обробка зображень

## 📝 Ліцензія

Датасет ліцензований під Creative Commons Attribution 4.0 (CC BY 4.0)

## 📞 Контакти

За питаннями звертайтеся до розробника проекту.

---

**Успіхів у тренуванні! 🚀**

